% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CommuteCA-package.R
\name{search_impedance_function}
\alias{search_impedance_function}
\title{Obtain calibrated impedance function}
\usage{
search_impedance_function(
  code,
  type = c("PCD", "CMA"),
  mode = NULL,
  use_education = FALSE,
  education = NULL,
  pcd_df = pcd_impedance_functions,
  cma_df = cma_impedance_functions,
  pr_df = pr_impedance_functions,
  pcd_edu_df = pcd_education_impedance_functions,
  cma_edu_df = cma_education_impedance_functions,
  pr_edu_df = pr_education_impedance_functions
)
}
\arguments{
\item{code}{A character or numeric vector of length 1: the PCD or CMA code.}

\item{type}{Either \code{"PCD"} or \code{"CMA"}. Defaults to \code{"PCD"}.}

\item{mode}{Optional character string naming one of the four modes
(\code{"Bike"}, \code{"Walk"}, \code{"Car-motorized"}, \code{"Public transit"}).
If \code{NULL} (default), all four modes are returned.}

\item{use_education}{logical \code{TRUE} or \code{FALSE}; if \code{TRUE}, use education-specific impedance functions
(with an \code{Education} column). If \code{FALSE} (default), use transportation mode-only functions.}

\item{education}{Optional character string specifying an education level.
Only used when \code{use_education = TRUE}. Selection between:
\code{"College or apprenticeship certificate"},
\code{"High school or no certificate"}, or \code{"University certificate or higher"}.
If \code{NULL} (default), all three education levels are returned.}

\item{pcd_df}{Transportation mode-only PCD data frame. Defaults to \code{pcd_impedance_functions}.}

\item{cma_df}{Transportation mode-only CMA data frame. Defaults to \code{cma_impedance_functions}.}

\item{pr_df}{Transportation mode-only provincial data frame. Defaults to \code{pr_impedance_functions}.}

\item{pcd_edu_df}{Mode-education-function PCD data frame.
Defaults to \code{pcd_education_impedance_functions}.}

\item{cma_edu_df}{Mode-education-function CMA data frame.
Defaults to \code{cma_education_impedance_functions}.}

\item{pr_edu_df}{Mode-education-function provincial data frame.
Defaults to \code{pr_education_impedance_functions}.}
}
\value{
A tibble with columns:
\item{code}{Input location code.}
\item{type}{Search type (\code{"PCD"} or \code{"CMA"}).}
\item{PwMode_label}{Transportation mode.}
\item{Education}{Education level (only if \code{use_education = TRUE}; otherwise \code{NA}).}
\item{Distribution}{Name of the fitted distribution.}
\item{est_1}{First parameter.}
\item{est_2}{Second parameter.}
\item{source}{Geographical level of the function: \code{"PCD"},\code{"CMA"}, or \code{"PR"}.}
}
\description{
For a given location (PCD or CMA), this function searches for impedance
parameters, first, at the most detailed level, and then falls back to higher levels:
PCD search: PCD => CMA => Province (CMACATEG).
CMA search: CMA => Province (CMACATEG).
}
\details{
It is possible to choose choose between transportation mode functions or mode-education functions.
}
\examples{
\dontrun{
# search by PCD using transportation mode-only functions (all modes)
search_impedance_function(3520)

# search by PCD with mode-education functions, all education levels
search_impedance_function(3520, use_education = TRUE)

# search by PCD with mode-education functions, filtered by one education level
search_impedance_function(3520,
                          use_education = TRUE,
                          education = "University certificate or higher")

# Search by CMA
search_impedance_function(537, type = "CMA")
}
}
