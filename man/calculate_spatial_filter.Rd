% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CommuteCA-package.R
\name{calculate_spatial_filter}
\alias{calculate_spatial_filter}
\title{Calculate a spatial filter}
\usage{
calculate_spatial_filter(
  spatial_data_frame,
  matrix_eigv,
  by = "ID",
  group_vars = NULL,
  response = "access",
  ev_prefix = "EV",
  threshold = 0.05
)
}
\arguments{
\item{spatial_data_frame}{A data frame containing the spatial variable (\code{response})
and the identifier (join key). Must contain a column named by \code{response} and a
column matching \code{by} (or the left side of \code{by} if it is named).}

\item{matrix_eigv}{A data frame containing the eigenvectors (columns
starting with \code{ev_prefix}) and an identifier (join key) matching the right side of \code{by}.}

\item{by}{A character string or a named character vector of length 1
specifying the join key(s). If a single string, it is used as the
column name in both tables. If named (e.g. \code{c("ID" = "CTUID")}),
the name is the column in \code{spatial_data_frame} and the value is the
column in \code{matrix_eigv}. Default: \code{"ID"}.}

\item{group_vars}{Optional character vector naming columns in
\code{spatial_data_frame} by which to divide the data before computing
the filter. For each unique combination of these variables, a
separate filter is built. If \code{NULL} (default), a single filter is
computed for all rows.}

\item{response}{Character string naming the response (e.g., \code{y}, dependent)
variable in \code{spatial_data_frame}. Default: \code{"access"}.}

\item{ev_prefix}{Character string giving the prefix of the eigenvector
columns in \code{matrix_eigv}. Default: \code{"EV"}.}

\item{threshold}{Numeric p-value threshold for including an eigenvector
in the filter (two-sided test). Default: \code{0.05}.}
}
\value{
A data frame with the following columns:
\itemize{
\item the identifier column (named as in \code{by} or its left side)
\item the response variable (named \code{response})
\item the spatial filter, named \verb{Sf_<response>}
\item any grouping columns specified in \code{group_vars} (if provided)
}
The rows are in the same order as the original \code{spatial_data_frame}.
}
\description{
This function builds a spatial filter by iteratively adding
eigenvectors to a linear model. Only eigenvectors with a p-value below a
threshold are retained and cumulatively added to the filter (default <= 0.05).
The function can optionally divide the data by one or more categorical variables
(e.g., mode, education level) and calculate separate filters for each subset.
The result is a single data frame containing the identifier, the response
variable, the computed filter, and the grouping columns (if any).
}
\details{
The function performs a forward selection procedure:
\enumerate{
\item Join \code{spatial_data_frame} with \code{matrix_eigv} on the specified key.
\item Initialize \code{Sf = 0} and \code{x = 1}.
\item For each eigenvector (in the order they appear in the joined
data), fit the model \code{response ~ x + Sf + EV_i}.
\item If the coefficient of \code{EV_i} has a p-value < \code{threshold},
add \code{coef(EV_i) * EV_i} to \code{Sf}.
\item Return a data frame with only the ID, response, filter, and groups.
}
The same procedure is applied independently to each group defined by
\code{group_vars}, and the results are row-bound into a single data frame.
}
\note{
The eigenvectors are assumed to have been pre-computed (e.g., from a
spatial weights matrix).
}
\examples{
\dontrun{
# Single group (no splitting)
result_df <- calculate_spatial_filter(spatial_data_frame = my_access,
  matrix_eigv = eigen_matrix)

# Split by transportation mode (column "PwMode")
result_by_mode <- calculate_spatial_filter(spatial_data_frame = SA_mode,
  matrix_eigv = matrix_eigv,
  by = "PRCDDA",
  group_vars = "PwMode",
  response = "SA_im")

# Split by combination of education and mode
result_by_edu_mode <- calculate_spatial_filter(spatial_data_frame = my_data,
  matrix_eigv = eigen_matrix,
  by = c("CTUID" = "ID"),
  group_vars = c("Education", "Mode"),
  response = "SA_i",
  ev_prefix = "EIG",
  threshold = 0.10)
}
}
\references{
This function is based on the study of Le Gallo, J., & Paez, A. (2013).

Le Gallo, J., & Paez, A. (2013). \emph{Using synthetic variables in
instrumental variable estimation of spatial series models.}.
\emph{Environment and Planning A}, \emph{45}(9), 2227-2242.
\doi{10.1068/a45443}
}
