% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CommuteCA-package.R
\name{theoretical_impedance}
\alias{theoretical_impedance}
\title{Generate theoretical impedance curves for combinations of categorical fields}
\usage{
theoretical_impedance(
  df_functions,
  travel_cost = 60,
  group_cols = c("PwMode", "PCD")
)
}
\arguments{
\item{df_functions}{A data frame containing at least columns: \code{distribution}, \code{est_1},
\code{est_2}, and the categorical grouping columns.}

\item{travel_cost}{A numeric value – the maximum travel cost. The function creates a
sequence from 1 to \code{travel_cost} in steps of 0.5.}

\item{group_cols}{A character vector naming the columns in \code{df_functions} that define
the unique impedance functions (e.g., \code{c("PwMode_label", "PCD")}). Defaults to
\code{c("PwMode_label", "PCD")} for backward compatibility.}
}
\value{
A data frame with columns: \code{t} (travel cost), \code{f} (impedance), and each of the
grouping columns.
}
\description{
This function creates a data frame of impedance values (\code{f}) for a sequence of travel
costs (\code{t}), based on distribution parameters stored in a dataframe. The user can
specify which categorical columns (e.g., \code{PwMode_label}, \code{PCD}, \code{Education}) define the unique
curves.
}
\examples{
\dontrun{
# Default: group by PwMode_label and PCD
curves <- theoretical_impedance(functions_df, travel_cost = 30)

# With three grouping columns
curves <- theoretical_impedance(functions_df, travel_cost = 30,
                                group_cols = c("PwMode_label", "PCD", "Education"))
}
}
