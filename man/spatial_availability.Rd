% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/CommuteCA-package.R
\name{spatial_availability}
\alias{spatial_availability}
\title{Calculate spatial availability (SA_ij) for a given mode and impedance}
\usage{
spatial_availability(df, origin, destination, pop, opp, mode, f)
}
\arguments{
\item{df}{A data frame containing origin, destination, population, opportunity,
mode, and impedance columns.}

\item{origin}{Unquoted column name identifying the origin zone (e.g., \code{PRCDDA}, \code{CTUID}, \code{from_id}).}

\item{destination}{Unquoted column name identifying the destination zone (e.g., \code{PWDA}, \code{to_id}).}

\item{pop}{Unquoted column name for the population (or labour force) at each origin.}

\item{opp}{Unquoted column name for the opportunities (e.g., jobs) at each destination.}

\item{mode}{Unquoted column name for the travel mode (used only for grouping).}

\item{f}{Unquoted column name containing the impedance value (e.g., \code{f} from \code{generate_impedance}).}
}
\value{
A data frame (the input \code{df} with additional columns: \code{sum_pop}, \code{f_c}, \code{f_p},
\code{sum_pa}, \code{f_t}, and \code{SA_ij} – the final spatial availability).
}
\description{
This function computes the spatial availability of opportunities (jobs, services, etc.)
from each origin to each destination (see Soukhov et al 2024, https://journals.plos.org/plosone/article?id=10.1371/journal.pone.0299077.)
}
\examples{
\dontrun{
accessibility_table \%>\%
  spatial_availability(origin = PRCDDA,
                       destination = PWDA,
                       pop = labour_force,
                       mode = PwMode_label,
                       opp = jobs,
                       f = f)
}
}
